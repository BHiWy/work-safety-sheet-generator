/**
 * Configuration object for the development environment.
 */
export const environment = {
  /**
   * Indicates whether the application is in production mode.
   * In this case, it is set to `false` for development.
   * @type {boolean}
   */
  production: false,
  /**
   * The base URL of the API server used by the application in this environment.
   * @type {string}
   */
  apiBaseUrl: 'http://localhost:8080'
}
